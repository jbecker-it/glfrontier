<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="1"
    android:versionName="1.0"
    android:installLocation="auto">

    <uses-feature android:glEsVersion="0x00020000" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- The abstract input layer this port drives is the same one a real
         gamepad uses, so a connected controller works with no extra code. -->
    <uses-feature
        android:name="android.hardware.gamepad"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true" >

        <!-- screenOrientation: Frontier's display is 320x200, a 1.6 aspect,
             so landscape is the only sensible orientation. sensorLandscape
             rather than landscape so the device can be held either way up.

             resizeableActivity: needed for foldables and multi-window.
             Without it the system letterboxes us into the folded aspect and
             never hands back the extra area when the device is opened.

             configChanges: every one of these must be handled by us rather
             than by an activity restart. Unfolding changes screenSize,
             smallestScreenSize and screenLayout at once, and if the activity
             were recreated the emulator state - the player's game - would go
             with it. SDL turns these into SDL_WINDOWEVENT_SIZE_CHANGED,
             which is what the renderer and the touch layout listen for. -->
        <activity
            android:name=".FrontierActivity"
            android:label="@string/app_name"
            android:alwaysRetainTaskState="true"
            android:launchMode="singleInstance"
            android:screenOrientation="sensorLandscape"
            android:resizeableActivity="true"
            android:configChanges="layoutDirection|locale|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|density|keyboard|keyboardHidden|navigation"
            android:preferMinimalPostProcessing="true"
            android:exported="true" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
        </activity>
    </application>

</manifest>
